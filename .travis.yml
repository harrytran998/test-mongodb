language: node_js
node_js:
  - '10'
sudo: required
services:
  - docker
  - mongodb
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version version-number
  - export PATH="$HOME/.yarn/bin:$PATH"
cache: yarn
script:
  - yarn
  - yarn test
